anrp,rate=13,0.165
print(f"{anrp} * {rate:.6f} = {anrp*rate:.6f}")

# anrp,rate=13,0.165
# print("%d * %.6f = %.6f" % (anrp,rate,anrp*rate))

# anrp,rate=13,0.165
# rhq=anrp*rate
# print("{0} * {1:.6f} = {2:.6f}".format(anrp,rate,rhq))
